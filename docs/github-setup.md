# GitHub Setup

Initialize Git, commit the project, create a GitHub repository, add the remote, and push.

```powershell
git init
git add .
git commit -m "Initial Bandhan Django ecommerce project"
git remote add origin https://github.com/YOUR-ORG/bandhan-django.git
git push -u origin main
```
