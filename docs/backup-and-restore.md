# Backup and Restore

Back up the database, uploaded media, `.env` values, and deployment configuration. Test restore into a staging database before relying on a backup process. Schedule database dumps and media syncs through cron or the host backup system.
