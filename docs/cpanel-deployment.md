# cPanel Deployment

Confirm the plan supports persistent Python/WSGI apps, virtual environments, pip, MySQL or PostgreSQL, environment variables, cron, SSH, custom domain, SSL, static files, and media storage. Use `passenger_wsgi.py` only on hosts that support Passenger Python applications.
